# Linux server
### Change user root
        sudo -u root -s
### setting ssh client
        sudo apt-get install openssh-client
        sudo apt-get install openssh-server
        sudo -s
        adduser anph